# Copy to .env and fill in. ring.toml refers to these as ${NAME}.
HELIUS_API_KEY=
# Browser origins the ring RPC answers, comma separated. Passkey reads need it.
RING_RPC_ALLOW_ORIGINS=http://localhost:3000
