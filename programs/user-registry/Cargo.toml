[package]
name = "zolana-user-registry"
version = "0.1.0"
description = "On-chain user registry: Solana pubkey to shielded payment keys and sync delegate"
edition.workspace = true
publish = false

[lib]
crate-type = ["cdylib", "lib"]
name = "zolana_user_registry"

[features]
default = []
bpf-entrypoint = []
no-entrypoint = []
cpi = ["no-entrypoint"]
test-sbf = []

[dependencies]
borsh = { workspace = true }
pinocchio = { workspace = true, features = ["cpi", "account-resize"] }
# `Address::find_program_address` is syscall-backed on-chain; the `curve25519`
# feature provides the host implementation for off-chain builds of this lib.
solana-address = { version = "2.6", features = ["curve25519"] }
thiserror = { workspace = true }
zolana-interface = { workspace = true }

[lints.rust.unexpected_cfgs]
level = "allow"
check-cfg = [
    'cfg(target_os, values("solana"))',
    'cfg(feature, values("frozen-abi", "no-entrypoint"))',
]
