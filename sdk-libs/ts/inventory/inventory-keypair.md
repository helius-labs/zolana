# Keypair crate inventory

Frozen baseline: `43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f` (`43fde8e4`). `@zolana/keypair` owns secret-bearing cryptography and exact cross-language vectors. Returned bytes are owned copies and browser roots use no Node globals.

Every current path below has one `inventory-active` marker used by the deterministic coverage check. A `not applicable` row is an explicit exclusion from implementation, but remains counted exactly once.

## Manifest and production source

| Frozen Rust path and symbol | Disposition | Target TypeScript | Export / responsibility | Observable behavior and invariants | Dependencies | Typed error mapping | Fixture | Required tests | Packet |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
<!-- inventory-active: sdk-libs/keypair/Cargo.toml -->
| [`sdk-libs/keypair/Cargo.toml`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/Cargo.toml) · `crate manifest and crypto dependency graph` | `port` | `@zolana/keypair` · `package.json` | Package boundary, features, and allowed production dependency graph; no runtime export. | Preserve crate boundary and feature intent; browser roots exclude Node/test dependencies and optional Rust sync/parallel features map to explicit TS adapters. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Frozen dependency/feature snapshot and clean packed-package graph. | Unit: config validation. Property: dependency rule scan. Integration: Node/browser import. E2E: packed consumer. | `P01` |
<!-- inventory-active: sdk-libs/keypair/src/constants.rs -->
| [`sdk-libs/keypair/src/constants.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/constants.rs) · `length constants; domain separators; P_CONST_SEC1` | `port` | `@zolana/keypair` · `src/constants.ts` | `BLINDING_LENGTH`, `DST_VIEW_ROOT`, `P256_PUBLIC_KEY_LENGTH`, `P_CONST_SEC1`, `SALT_LENGTH`, `SHIELDED_PUBLIC_KEY_LENGTH`, `VIEW_TAG_LEN`, one per `pub` item of the Rust module. The `INFO_*` labels and the HPKE prefixes stay internal because Rust keeps them `pub(crate)`. | length constants; domain separators; P_CONST_SEC1 preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/constants.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/encryption.rs -->
| [`sdk-libs/keypair/src/encryption.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/encryption.rs) · `ECDH, HKDF, AES-CTR slot primitives` | `internal` | `@zolana/keypair` · `src/encryption.ts` | Internal implementation of ECDH, HKDF, AES-CTR slot primitives; callers reach it through the package allowlist. | ECDH, HKDF, AES-CTR slot primitives preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/encryption.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/error.rs -->
| [`sdk-libs/keypair/src/error.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/error.rs) · `KeypairError` | `port` | `@zolana/keypair` · `src/error.ts` | Implements KeypairError; public only where `public-exports.md` declares it. | KeypairError preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/error.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/hash.rs -->
| [`sdk-libs/keypair/src/hash.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/hash.rs) · `poseidon; hash_field; split_be_128; sha256; owner_hash` | `port` | `@zolana/keypair` · `src/hash.ts` | Ported: `hashField`, `ownerHash`, `sha256Be`, `sha256Bytes` and `splitBigEndian128` are re-exported by name from the package root, so callers reach them directly rather than through another module's allowlist. | poseidon; hash_field; split_be_128; sha256; owner_hash preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/hash.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/lib.rs -->
| [`sdk-libs/keypair/src/lib.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/lib.rs) · `crate-root keypair pub use allowlist` | `port` | `@zolana/keypair` · `src/index.ts` | Explicit export/module responsibility for crate-root keypair pub use allowlist; only names allowlisted in `public-exports.md` are public. | crate-root keypair pub use allowlist preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/lib.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/merge.rs -->
| [`sdk-libs/keypair/src/merge.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/merge.rs) · `MERGE_INFO; encrypt_verifiable; decrypt_verifiable; contribution/hash` | `port` | `@zolana/keypair` · `src/merge.ts` | Implements MERGE_INFO; encrypt_verifiable; decrypt_verifiable; contribution/hash; public only where `public-exports.md` declares it. | MERGE_INFO; encrypt_verifiable; decrypt_verifiable; contribution/hash preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/merge.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/nullifier_key.rs -->
| [`sdk-libs/keypair/src/nullifier_key.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/nullifier_key.rs) · `NullifierKey` | `port` | `@zolana/keypair` · `src/nullifier-key.ts` | Implements NullifierKey; public only where `public-exports.md` declares it. | NullifierKey preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/nullifier_key.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/pubkey.rs -->
| [`sdk-libs/keypair/src/pubkey.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/pubkey.rs) · `P256Pubkey; PublicKey; SignatureType` | `port` | `@zolana/keypair` · `src/pubkey.ts` | Implements P256Pubkey; PublicKey; SignatureType; public only where `public-exports.md` declares it. | P256Pubkey; PublicKey; SignatureType preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/pubkey.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/shielded.rs -->
| [`sdk-libs/keypair/src/shielded.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/shielded.rs) · `ShieldedAddress; CompressedShieldedAddress; ShieldedKeypair` | `port` | `@zolana/keypair` · `src/shielded.ts` | Implements ShieldedAddress; CompressedShieldedAddress; ShieldedKeypair; public only where `public-exports.md` declares it. | ShieldedAddress; CompressedShieldedAddress; ShieldedKeypair preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/shielded.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/signing_key.rs -->
| [`sdk-libs/keypair/src/signing_key.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/signing_key.rs) · `SigningKey` | `port` | `@zolana/keypair` · `src/signing-key.ts` | Implements SigningKey; public only where `public-exports.md` declares it. | SigningKey preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/signing_key.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/traits/mod.rs -->
| [`sdk-libs/keypair/src/traits/mod.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/traits/mod.rs) · `ShieldedKeypairTrait; ViewingKeyTrait re-exports` | `port` | `@zolana/keypair` · `src/traits/index.ts` | Explicit export/module responsibility for ShieldedKeypairTrait; ViewingKeyTrait re-exports; only names allowlisted in `public-exports.md` are public. | ShieldedKeypairTrait; ViewingKeyTrait re-exports preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/mod.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/traits/shielded_keypair.rs -->
| [`sdk-libs/keypair/src/traits/shielded_keypair.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/traits/shielded_keypair.rs) · `ShieldedKeypairTrait` | `port` | `@zolana/keypair` · `src/traits/shielded-keypair.ts` | Implements ShieldedKeypairTrait; public only where `public-exports.md` declares it. | ShieldedKeypairTrait preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/shielded_keypair.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/traits/view_key.rs -->
| [`sdk-libs/keypair/src/traits/view_key.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/traits/view_key.rs) · `ViewingKeyTrait` | `port` | `@zolana/keypair` · `src/traits/view-key.ts` | Implements ViewingKeyTrait; public only where `public-exports.md` declares it. | ViewingKeyTrait preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/view_key.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |
<!-- inventory-active: sdk-libs/keypair/src/viewing_key.rs -->
| [`sdk-libs/keypair/src/viewing_key.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/src/viewing_key.rs) · `ViewingKey; random_salt; random_blinding` | `port` | `@zolana/keypair` · `src/viewing-key.ts` | Implements ViewingKey; random_salt; random_blinding; public only where `public-exports.md` declares it. | ViewingKey; random_salt; random_blinding preserves exact key lengths/domain separators, P256/Ed25519 rail separation, owned secret/output bytes, deterministic vectors, curve/field validation, and best-effort secret destruction where exposed. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | `KeypairError` (`INVALID_PUBLIC_KEY`, `INVALID_SECRET_KEY`, `ZERO_SCALAR`, `INVALID_SIGNATURE_TYPE`, `HKDF`, `POSEIDON`, `FIELD_ELEMENT_TOO_LONG`) with details/cause. | Rust-generated `fixtures/keypair/viewing_key.json` with logical input, exact bytes/value, and error mutations. | Unit: success and every error branch. Property: length/range/order/tamper invariants. Integration: Rust fixture/real dependency boundary. E2E: owning workflow where public. | `P04` |

## BDD specifications and test support

| Frozen Rust path and symbol | Disposition | Target TypeScript | Export / responsibility | Observable behavior and invariants | Dependencies | Typed error mapping | Fixture | Required tests | Packet |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
<!-- inventory-active: sdk-libs/keypair/tests/bdd.rs -->
| [`sdk-libs/keypair/tests/bdd.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/bdd.rs) · `test module: bdd` | `test-only` | `@zolana/keypair` · `test/bdd.test.ts` | Non-public evidence for test module `bdd`; no runtime export. | Provide isolated deterministic actor/world state and lifecycle for scenarios; no production export or protocol logic duplication. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/bdd.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/features/hashing.feature -->
| [`sdk-libs/keypair/tests/features/hashing.feature`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/features/hashing.feature) · `Feature scenarios: hashing` | `port` | `@zolana/keypair` · `test/hashing.test.ts` | Non-public evidence for Feature scenarios: hashing; no runtime export. | Assert SHA variants, field encoding, public-key fields, owner-hash stability, and P256/Ed25519 domain separation. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Rust scenario `hashing.feature` plus emitted expected bytes/state in `fixtures/hashing.json`. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/features/nullifier.feature -->
| [`sdk-libs/keypair/tests/features/nullifier.feature`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/features/nullifier.feature) · `Feature scenarios: nullifier` | `port` | `@zolana/keypair` · `test/nullifier.test.ts` | Non-public evidence for Feature scenarios: nullifier; no runtime export. | Assert deterministic nullifier key/public key derivation and binding to UTXO hash and leaf index; mutations must differ. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Rust scenario `nullifier.feature` plus emitted expected bytes/state in `fixtures/nullifier.json`. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/features/pubkey.feature -->
| [`sdk-libs/keypair/tests/features/pubkey.feature`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/features/pubkey.feature) · `Feature scenarios: pubkey` | `port` | `@zolana/keypair` · `test/pubkey.test.ts` | Non-public evidence for Feature scenarios: pubkey; no runtime export. | Assert SEC1 P256 and scheme-tagged public-key parsing, prefixes, padding, zero sentinel, curve points, and owner fields. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Rust scenario `pubkey.feature` plus emitted expected bytes/state in `fixtures/pubkey.json`. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/features/shielded.feature -->
| [`sdk-libs/keypair/tests/features/shielded.feature`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/features/shielded.feature) · `Feature scenarios: shielded` | `port` | `@zolana/keypair` · `test/shielded.test.ts` | Non-public evidence for Feature scenarios: shielded; no runtime export. | Assert key composition, shielded/compressed addresses, owner hashes, tags, signatures, nullifiers, and both ownership rails. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Rust scenario `shielded.feature` plus emitted expected bytes/state in `fixtures/shielded.json`. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/features/signing.feature -->
| [`sdk-libs/keypair/tests/features/signing.feature`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/features/signing.feature) · `Feature scenarios: signing` | `port` | `@zolana/keypair` · `test/signing.test.ts` | Non-public evidence for Feature scenarios: signing; no runtime export. | Assert deterministic P256/Ed25519 signatures, verification, secret round trips, scalar validation, and message/key/signature tamper rejection. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Rust scenario `signing.feature` plus emitted expected bytes/state in `fixtures/signing.json`. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/features/transaction.feature -->
| [`sdk-libs/keypair/tests/features/transaction.feature`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/features/transaction.feature) · `Feature scenarios: transaction` | `port` | `@zolana/keypair` · `test/transaction.test.ts` | Non-public evidence for Feature scenarios: transaction; no runtime export. | Assert ECDH/HKDF/AES slot encryption, slot separation, sender/recipient agreement, stranger rejection, and golden decryption. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Rust scenario `transaction.feature` plus emitted expected bytes/state in `fixtures/transaction.json`. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/features/viewing.feature -->
| [`sdk-libs/keypair/tests/features/viewing.feature`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/features/viewing.feature) · `Feature scenarios: viewing` | `port` | `@zolana/keypair` · `test/viewing.test.ts` | Non-public evidence for Feature scenarios: viewing; no runtime export. | Assert view-root derivation, ECDH, sender/request/merge/bootstrap tag streams, transaction viewing keys, and counter separation. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Rust scenario `viewing.feature` plus emitted expected bytes/state in `fixtures/viewing.json`. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/common.rs -->
| [`sdk-libs/keypair/tests/steps/common.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/common.rs) · `test module: common` | `test-only` | `@zolana/keypair` · `test/steps/common.test.ts` | Non-public evidence for test module `common`; no runtime export. | Provide only `common` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/common.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/hashing.rs -->
| [`sdk-libs/keypair/tests/steps/hashing.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/hashing.rs) · `test module: hashing` | `test-only` | `@zolana/keypair` · `test/steps/hashing.test.ts` | Non-public evidence for test module `hashing`; no runtime export. | Provide only `hashing` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/hashing.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-exclusion: sdk-libs/keypair/tests/steps/mod.rs -->
| [`sdk-libs/keypair/tests/steps/mod.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/mod.rs) · `test module: mod` | `not applicable` | `@zolana/keypair` · `test/steps/mod.test.ts` | Rust-only test runner/module wiring; no TypeScript runtime export or source file. | Provide only `mod` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | No byte fixture; coverage is the runner-to-Vitest replacement decision. | Unit: inventory exclusion check. Property/integration/E2E: not applicable to runner wiring; covered by the mapped scenario suites. | `P00` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/nullifier.rs -->
| [`sdk-libs/keypair/tests/steps/nullifier.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/nullifier.rs) · `test module: nullifier` | `test-only` | `@zolana/keypair` · `test/steps/nullifier.test.ts` | Non-public evidence for test module `nullifier`; no runtime export. | Provide only `nullifier` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/nullifier.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/pubkey.rs -->
| [`sdk-libs/keypair/tests/steps/pubkey.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/pubkey.rs) · `test module: pubkey` | `test-only` | `@zolana/keypair` · `test/steps/pubkey.test.ts` | Non-public evidence for test module `pubkey`; no runtime export. | Provide only `pubkey` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/pubkey.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/shielded.rs -->
| [`sdk-libs/keypair/tests/steps/shielded.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/shielded.rs) · `test module: shielded` | `test-only` | `@zolana/keypair` · `test/steps/shielded.test.ts` | Non-public evidence for test module `shielded`; no runtime export. | Provide only `shielded` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/shielded.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/signing.rs -->
| [`sdk-libs/keypair/tests/steps/signing.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/signing.rs) · `test module: signing` | `test-only` | `@zolana/keypair` · `test/steps/signing.test.ts` | Non-public evidence for test module `signing`; no runtime export. | Provide only `signing` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/signing.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/transaction.rs -->
| [`sdk-libs/keypair/tests/steps/transaction.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/transaction.rs) · `test module: transaction` | `port` | `@zolana/keypair` · `test/steps/transaction.test.ts` | Non-public evidence for test module `transaction`; no runtime export. | Provide only `transaction` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/transaction.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |
<!-- inventory-active: sdk-libs/keypair/tests/steps/viewing.rs -->
| [`sdk-libs/keypair/tests/steps/viewing.rs`](https://github.com/helius-labs/zolana/blob/43fde8e45d3b1d78aa4c7517a07d6a9675d9bf9f/sdk-libs/keypair/tests/steps/viewing.rs) · `test module: viewing` | `test-only` | `@zolana/keypair` · `test/steps/viewing.test.ts` | Non-public evidence for test module `viewing`; no runtime export. | Provide only `viewing` scenario setup/assertions and deterministic fixtures; production logic must remain in package modules and direct TS tests. | Audited P-256/Ed25519, Poseidon, SHA-256, HKDF, AES-CTR, CSPRNG/zeroization; no SDK dependency. | Assert the package error code/details/cause named by the exercised branch; harness failures are not public errors. | Deterministic inputs and assertions extracted from `sdk-libs/keypair/tests/steps/viewing.rs`; preserve named regression seeds where present. | Direct unit/scenario port; add property mutation where data-shaped, integration with real dependency where I/O/proving is involved, and map to action- or instruction-level E2E when observable. | `P04` |

