# Vendored Light test-validator accounts

These account JSON fixtures are loaded by `zolana test-validator` with
`--account-dir`. They mirror the account fixtures shipped in
`@lightprotocol/zk-compression-cli@0.28.4` so forester e2e and program-test
runs do not depend on a globally installed npm package.
