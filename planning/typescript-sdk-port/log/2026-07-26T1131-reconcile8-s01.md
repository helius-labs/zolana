# 2026-07-26 11:31 UTC | reconciler pass eight: S01 closed, adverse denominator empty | S01

- Baseline: HEAD `df4d2fab`, the merge of `port/s01` into `ts-sdk-port`, checked out as
  `port/reconcile-s01`. Fix commit under test: `088c9b0d`
- Worker: reconciler, sole writer of `review-checklist.md`
- Scope: `planning/` only. No source under `sdk-libs/`, `programs/`, `program-libs/`,
  `prover/`, `services/` or `xtask/` was touched
- Folded: [row-updates/s01.md](../row-updates/s01.md)
- Gates: `node sdk-libs/ts/config/review-checklist-check.mjs` and
  `node sdk-libs/ts/config/pkp-entry-gate.mjs` both run after this entry landed

- Verdict: `S01` reaches PARITY

Counted from the tables with `awk` over the Status and Verdict columns rather
than adjusted from the previous figure: **135 `done`/`PARITY`, 0 adverse**, 7
`done`/`NOT_APPLICABLE` and 3 `needs_re_review`/`NOT_APPLICABLE`, summing to 145.
The adverse denominator falls from 1 to 0. Criterion 2 of the cryptographic
certification entry gate is clear; criterion 4 (CI) remains a separate worker.

## What was checked at HEAD, not credited

Pass seven correctly refused an earlier false close that never touched
`smart-account-client/`. This pass re-derived the six acceptance shapes against
Rust `sdk-libs/smart-account-client/src/lib.rs` and TypeScript
`sdk-libs/ts/smart-account-client/src/instructions.ts` at `df4d2fab`.

1. Empty create signers: Rust `create_smart_account_ix` serializes an empty
   `Vec` with no check (`lib.rs:118-156`). TypeScript constructs
   (`instructions.ts:37-93`; no empty-signer throw; pinned in
   `boundaries.test.ts`).
2. Threshold 0: Rust takes `threshold: u16` with no range check against the
   signer count. TypeScript accepts `threshold: 0` after the `u16` domain check
   only.
3. Threshold above signer count: same path; TypeScript accepts
   `threshold: 2` with one signer.
4. Duplicate create or execute signers: Rust copies the slice / pushes each key
   with no uniqueness walk. TypeScript constructs both shapes; execute keeps
   both account metas.
5. Create or execute data past 1232 bytes: Rust has no builder-level packet
   limit. TypeScript dropped `TRANSACTION_SIZE_LIMIT` from this package; both
   builders construct past 1232. Owner-ruled `checkedTransactionSize` remains at
   its three production call sites outside this package
   (`client/src/client.ts:739`, `wallet/src/internal.ts:206`,
   `test-kit/src/user-registry.ts:383`).
6. Inner data length `0x10000`: Rust writes `ix.data.len() as u16` then the full
   bytes (`lib.rs:269-271`), so the prefix is zero and the buffer stays.
   TypeScript writes `inner.data.length & 0xffff` then the full bytes
   (`instructions.ts:181-183`). `boundaries.test.ts` pins the zero prefix and
   the first and last payload bytes.

Shared `u8` count and compiled-index refusals survive: Rust `checked_u8`,
TypeScript `assertCount` / `SMART_ACCOUNT_TOO_MANY_ACCOUNTS`. The fix did not
overshoot into accepting a count or compiled index Rust refuses.

## Refusal history

Pass seven kept `S01` at `DIVERGENT` because the TypeScript-only refusals were
still in the tree. `088c9b0d` removed them; this entry supersedes the pass-seven
`stays DIVERGENT` attribution.
