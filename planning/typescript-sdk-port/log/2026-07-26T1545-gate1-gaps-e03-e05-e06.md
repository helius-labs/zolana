# 2026-07-26 15:45 UTC | gate1-gaps closes E03, E05, E06 | E03 E05 E06

- Baseline: worktree `/Users/tilohelius/Workspace/zolana-wt-gate1-gaps`, branch
  `port/gate1-gaps`
- Worker: gate1-gaps
- Scope: `@zolana/interface` surface, event disposition tests, checklist rows
- Evidence: `interface/test/vectors/event-disposition.test.ts`; deletion of the
  unreachable `OutputUtxo` type from `interface/src/index.ts`

- Verdict: `E03` `NOT_APPLICABLE`
- Verdict: `E05` `NOT_APPLICABLE`
- Verdict: `E06` `NOT_APPLICABLE`

## E03

Unreachability was settled before this session. Disposition chosen: delete the
dead TypeScript type. Rust event `OutputUtxo` remains a `GeneralEvent` field and
an interface re-export; the transact instruction uses `TransactOutput`.
`ProofOutputUtxo` in `@zolana/transaction` is unrelated.

## E05 / E06

Confirmed with a committed test that fails if `GeneralEvent` / `EventKind` /
`encode_event_*` appear in the interface, indexer-api, or api trees, and that
pins `program-test = []` off the event crate default feature set. Spec
`Option` vs zeroed-array divergence on `tx_viewing_pk`/`salt` stays a
spec-side note.
