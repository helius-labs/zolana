# 2026-07-26 14:45 UTC | W03 | `sdk-libs/wallet/src/actions/submit.rs`

- Baseline: worktree `zolana-ts-w03` on `port/w03`
- Worker: W03 close + reconciler
- Explanation: The two arms the `a3ecf6a9` re-review left untested now have a regenerable Rust oracle and a TypeScript replay. `validate_merge_submission`'s signing-rail, nullifier, and viewing rejections, and `ensure_proofs_match_submit_tree`'s wrong-state / wrong-nullifier indexer responses, are recorded in `sdk-libs/ts/vectors/wallet-submit-v1.json` by `cargo run -p xtask --bin wallet-submit` and replayed by `wallet/test/vectors/wallet-submit.test.ts`.
- Evidence: Rust and TypeScript agree on `MergeSigningKeyMismatch` / `WALLET_MERGE_SIGNING_KEY_MISMATCH`, `MergeNullifierKeyMismatch` / `WALLET_MERGE_NULLIFIER_KEY_MISMATCH`, and `MergeViewingKeyMismatch` / `WALLET_MERGE_VIEWING_KEY_MISMATCH` (with `owner` detail). Indexer proofs that name another state or nullifier tree are refused as `WALLET_MERGE_TREE_MISMATCH` before the prover runs, including through `ZolanaClient.proveMerge` after `propagateAssemblerCause` stopped wrapping that wallet code as `CLIENT_UNEXPECTED`. Detail: `row-updates/w03.md`.
- Verdict: `PARITY`
- Row transition: `done` / `PARITY` retained; untested-arm residual closed
