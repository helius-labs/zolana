# 2026-07-25 21:05 UTC | keypair parity batch judged and folded into the table | `sdk-libs/keypair/`

- Baseline: HEAD `bffa530c`; fixture `keypair-parity-v1.json` at `8d644562`; Rust drift `sdk-libs/keypair` changed in this batch, see below
- Worker: Opus 5 reconciliation subagent, reading commits `8d644562`, `d80af2ac`, `797047a4`; no `row-updates` file exists
- Explanation: The keypair worker was cut off before writing a self-report, so there is no batch verdict to fold and no batch count to check. Every verdict below is mine, taken from the commits, the fixture, and the two test files, and the ten rows that close do so on tests I ran rather than on a report I read. This is the batch that had the most adverse rows going in, ten `needs_fix` of fourteen with five `DIVERGENT`, and it is the one that changed the most source: the interface batch changed no file under `interface/src`, and the program-libs batch changed one line of one file.
- Evidence: `sdk-libs/keypair/tests/parity_vectors.rs` links against the real `zolana-keypair` crate and writes `sdk-libs/ts/vectors/keypair-parity-v1.json`; `keypair/test/vectors/keypair-parity.test.ts` reads that fixture and holds TypeScript to it, and `keypair/test/api-surface.test.ts` holds the published surface to an explicit ledger per entry point. I ran `npx vitest run` in `sdk-libs/ts/keypair`: both files pass. I ran `node sdk-libs/ts/config/browser-check.mjs keypair`, which passes, and `node sdk-libs/ts/config/pack-check.mjs keypair`, which fails; the failure is not this port's and is treated as such below. I read the Rust traits under `sdk-libs/keypair/src/traits/` against their TypeScript interfaces directly, because that is where the batch's own work is least complete.

## Ten rows close on parity

- Verdicts: `PARITY` for `K01`, `K02`, `K03`, `K04`, `K05`, `K06`, `K07`, `K08`, `K09`, `K10`

Five of these were `DIVERGENT`, and in each case the divergence was repaired rather than argued away. The tagged public key is 34 bytes in its type as well as at runtime, and the fix is checked in the only way a type-level claim can be: `api-surface.test.ts` assigns the key to `Bytes34`, which would not typecheck against the old declaration, and rejects a 33-byte value where a tagged key is required. The five collapsed error distinctions are restored and held by a two-way mapping, so a code that stops claiming a Rust variant fails and a Rust variant that loses its code fails too. Poseidon is exported, bounded to Rust's `1..=12` arity, and compared at every arity; the extra unsafe hash helpers were deleted rather than hidden, and the three deleted names are pinned as values that must not reappear. `symmetricApply` is exported, and in the order this row demanded: Rust's `pack_info` silently mis-encoded a label longer than its two field limbs, that was fixed to return `KeypairError::InfoTooLong` above `MAX_INFO_LEN` (62), and only then was the capability ported with the bound carried across.

Two of the rows close partly on rulings that predate this batch. `K06`'s owner-hash conflict was settled by the G7-1 spec amendment at `1d6b9873`, and `K02`'s signature-acceptance question by G2-1 and G2-2 at `65100a09`. G2-1 is now pinned by its own test rather than resting on the ruling: the high-`s` twin of a valid signature verifies, because the circuit range-checks `s` against the curve order alone, so a `lowS: true` verifier would reject a signature the protocol accepts. That is the rare case where the stricter implementation is the wrong one, and the test states why.

Two closures rest on tests of a kind worth naming, because they prove agreement where an ordinary round-trip proves nothing. The encryption row compares what Rust *recovers* from a truncated, extended, tampered, wrong-slot, and wrong-salt ciphertext, which for a stream cipher is a plausible-looking wrong plaintext rather than an error; pinning Rust's exact garbage is the only way to know the two languages fail identically. The error row asserts that a `cause` is non-enumerable, so it survives on the instance for a debugger and does not reach `JSON.stringify`, and that a detail outside the closed set is dropped rather than carried.

`K01` closes with a documentation residue that is not behaviour: [inventory-keypair.md](../inventory-keypair.md) still dispositions `constants.rs` as `internal` and still names `fixtures/keypair/*.json` paths that no longer exist. The constants are exported and compared against Rust's own values, and the labels that should stay internal are asserted absent from the published surface, so the disposition question is answered in code. Correcting the inventory belongs to its owner.

## Four rows stay open, all four on the traits surface

- Verdicts: `PARTIAL` for `K11`, `K12`, `K13`, `K14`

The batch's own weakest work is here, and each row keeps a specific residue rather than a general doubt.

`K11`. `ViewingKeyLike` grew from two methods to all fourteen Rust declares, every one of them accepting a promise so a backend can satisfy it, and `ShieldedKeypair` implements it as Rust's `impl ViewingKeyTrait for ShieldedKeypair` does. The interfaces are exercised, not merely declared: an async `RemoteBackend` satisfies both, a slot ciphertext round-trips through the interface alone, and a full keypair stands in for a viewing-key backend. The row's third finding is untouched. `transaction/src/wallet/sync.ts`, `transaction/src/serialization/codecs.ts`, and `wallet/src/sync.ts` still take the concrete `ViewingKey`, so a backend that now typechecks cannot be handed to the sync or codec paths that would use it. Those files are owned by the transaction and wallet rows.

`K12`. The six omitted capabilities are present and the async facade is consumed. Rust's malformed-P256-sign panic was fixed in the direction the row asked for, by adding `try_sign` to `ShieldedKeypairTrait`. The other half was not: `nullifier_key()` still clones the nullifier key out of the trait, while TypeScript offers `nullifierPublicKey()` alone. TypeScript is the safer surface and the narrowing is deliberate and documented, but a trait that hands out secret material in one language and not the other is not parity, and which side moves is an owner's decision.

`K13`. This row is repointed, from `index.ts` to `keypair/src/traits/index.ts`, which is its actual counterpart now that one exists. The subpath re-exports both interfaces, is declared in the export map and registered in `config/packages.mjs`, and is asserted type-only, mirroring `traits/mod.rs`, which exports no runtime item either. Two residues remain: there is no trait-specific fixture, and the packed-package gate fails.

`K14`. Every missing-surface finding it named is closed on the row that owns it, and the exact root and subpath allowlists exist as literal sorted lists. Its two other findings stand: the stale inventory metadata recorded under `K01`, and the tarball and consumer allowlists, which cannot be closed while the packed gate fails.

That gate failure is worth stating precisely, because it would be easy to charge to this port. `node sdk-libs/ts/config/pack-check.mjs keypair` fails with `packed browser bundle contains globalThis.process`, while `browser-check.mjs keypair`, which bundles from source, passes. `merkle-tree` fails identically, and no runtime dependency in the workspace tree contains that string, so the offender enters through the fresh install the pack check performs into a temporary directory. It is a defect in the packed-artifact gate or in a newly resolved dependency, it blocks two packages rather than one, and it belongs to the gate owner.

- Gap and smallest fix: `K11`, widen the three higher-package signatures to `ViewingKeyLike`, which is a transaction- and wallet-row change. `K12`, an owner ruling on whether `ShieldedKeypairTrait::nullifier_key` should stop returning secret material. `K13`, `K14`, resolve the `globalThis.process` failure in the packed-artifact gate, and correct the keypair section of [inventory-keypair.md](../inventory-keypair.md)
- Row transitions: `needs_fix -> done` for `K01` through `K10`; `needs_fix -> needs_re_review` for `K11`, `K12`, `K13`, `K14`, each with its verdict moved off `PARTIAL`-with-open-findings or `DIVERGENT` to a `PARTIAL` that names one residue
- Progress: `58/145` after this entry; the keypair package is `10 of 14` closed
- Exact next file: `M01 sdk-libs/merkle-tree/src/lib.rs`
- Full SDK parity claim: unsupported. Three packages now rest on generated Rust oracles, and the rows that remain open in them are open for reasons a reader can act on rather than for want of evidence
