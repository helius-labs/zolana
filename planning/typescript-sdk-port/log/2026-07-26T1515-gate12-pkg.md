# 2026-07-26T1515 gate12-pkg

Worker: gate12-pkg on `port/gate12-pkg`.

## Gate 1

- Restated the full-SDK gate for the eleven workspace packages from root
  `package.json` (`hasher` and `test-kit` were the two uncounted under "nine").
- G9-4: verified already closed by `test:browser-runtime` ([browser.md](../row-updates/browser.md)); checked the package-gate box.
- G6-2: landed `aliasing-census.test.ts` and a copying `CompressedShieldedAddress.ownerHash`; checked the package-gate box.
- F041: publish metadata on all eleven manifests; unused deps removed; client's undeclared `@noble/*` declared; `checkDependencies` inspects `src` imports.
- Top-level gate 1 left unchecked: remaining package-gate bullets not walked per package this run.

## Gate 2

- `KEYPAIR_HASH` empty-slice / public-key Poseidon sites already `KEYPAIR_POSEIDON`; verified, no change.
- Promise: `userRecordAddress` already sync; no other vestigial sync-as-async found; `ShieldedKeypairLike` Promise retained per Q17.
- `address-append`: builder withdrawal confirmed; gate text carves it as unsupported.
- G2 compression left to `port/g2`; gate 2 unchecked.

Evidence: [row-updates/gate12-pkg.md](../row-updates/gate12-pkg.md).
