# Curator rings a policy can read a list from, one table per cluster.
# A curator joins by pull request and names only the lists it serves from
# its own entries.
#
# [devnet.compliance-desk]
# program = "<ring program id>"
# lists = ["block", "frozen"]
# entries_tree = "<entries tree>"

[localnet]

[devnet]
