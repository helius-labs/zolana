//! Committed Groth16 verifying keys.
//!
//! The files here are machine-generated from the gnark setup output
//! (`just regen-custom-ring-keys`) and must never be hand-edited: gnark's setup
//! is randomized, so a regenerated key does not match the released proving key.
pub mod auditor_key_encryption;
