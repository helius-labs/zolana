# light-verifier

Groth16 proof verifier for Light Protocol v2 circuits.

This crate owns the shared proof type, verifier errors, v2 verifying keys, and generic verification helpers used by batched Merkle tree updates and future circuit integrations.
